trigger:
- none

pool:
  vmImage: 'ubuntu-latest'

variables:
- group: GitHubSync

steps:
- task: gitsync@1
  inputs:
    GitRepoUrl: 'https://$(GitHubPAT)@github.com/$(GitHubOrg)/$(GitHubRepo).git'
